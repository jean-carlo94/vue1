//import { render } from '@testing-library/vue'
//import MyComponent from './MyComponent.vue'

test('it should work', () => {
  let value = 5;

    //Estimulo
    value = value + 2

    // Observar Pruebas
    expect( value ).toBe( 10 )
})

